---
import ProjectCard from "../components/ProjectCard.astro";
import Footer from '@/components/Footer.astro'
import CardPost from "../components/CardPost.astro";
import CoreLayout from "../layouts/CoreLayout.astro";
import type { Project } from "../models/Project";
import {projectsData} from '../data/projectsData'
import { getCollection } from "astro:content";
import GithubIcon from "@/components/icons/GithubIcon.astro";
import LinkedinIcon from "@/components/icons/LinkedinIcon.astro";
import TwitterIcon from "@/components/icons/TwitterIcon.astro";
const lastProjects: Project[] = projectsData.slice(0, 3)
const blogAllEntries = await getCollection('blog');
const blogEntries = blogAllEntries.slice(0, 3) // TODO: sort by date

---

<CoreLayout  title="Oscar Moreira | Home">
  <div class="flex flex-col gap-28">
    <section class="flex flex-col gap-6">
      <div class="flex flex-col md:flex-row gap-2 font-bold text-4xl md:text-5xl mt-20">
        <span>Hey, soy</span>
        <h1 class="text-emerald-500">Oscar Moreira 🌴</h1>
      </div>
      <p class="text-xl text-stone-800 font-normal leading-8">
        Software Engineer, de Tenerife, Islas Canarias 🇮🇨.
      </p>
      <div class="flex gap-4">
        <a href="https://twitter.com/oscarmoreira" target="_blank" rel="noopener noreferrer" aria-label="Follow me on Twitter" class="text-emerald-600 hover:text-stone-900"><TwitterIcon /></a>
        <a href="https://github.com/omorest" target="_blank" rel="noopener noreferrer" aria-label="Follow me on Github" class="text-emerald-600 hover:text-stone-900"><GithubIcon /></a>
        <a href="https://github.com/omorest" target="_blank" rel="noopener noreferrer" aria-label="Follow me on Linkedin" class="text-emerald-600 hover:text-stone-900"><LinkedinIcon /></a>
      </div>
    </section>
    
    
    <section class="flex flex-col gap-10">
      <h2 class="font-semibold text-3xl">Últimos posts</h2>
      <!-- <a href="/blog" class="font-normal text-stone-500">ver más</a> -->
      <div class="w-full h-[1px] bg-stone-300"></div>
      <div class="flex flex-wrap gap-14 justify-center md:justify-start">
        {
          blogEntries.map(post => <CardPost post={{...post.data, slug: post.slug}} />)
        }
      </div>
    </section>


    <section class="flex flex-col items-start gap-10">
      <h2 class="font-semibold text-3xl">Últimos projectos</h2>
      <div class="w-full h-[1px] bg-stone-300"></div>
      <div class="flex w-full">
        {
          lastProjects.map(project => <ProjectCard project={project} />)
        }
      </div>
    </section>
  </div>
</CoreLayout>